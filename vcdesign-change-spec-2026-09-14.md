# VCDesign 変更仕様

- 発行日: 2026-09-14
- 対象: VCDesign 本体（vcdesign.org/ja）、CCP（VCDesign-org/CCP）
- 種別: 位置づけ変更を含む構造修正
- 優先度: 高（項目1〜4は相互依存。単独適用不可）

---

## 0. 変更の背景

「ループエンジニアリング」（2026年6月に提唱されたAI駆動開発の手法。人間がプロンプトを打つ代わりに、エージェントに指示を出し続けるループを設計する）との突き合わせから、VCDesignの独自性がどこにあるかの切り分けが変わった。

確認した事実:

- ループエンジニアリングが成立しているのは、ループ内で成否が機械検証できる領域に限られる
- 言説が規範とする5要素（機械検証可能な停止条件／永続状態ファイル／検証サブエージェント／トークン予算／人へのエスカレーション点）のうち、実測では状態ファイルと検証者分離がほぼ実装されていない
- 本番エージェントは意図的に自律度を絞られている（大半が人手評価に依存）
- 推進側自身がボトルネックを「モデルではなく検証者」と認めている

結論: VCDesignの記述形式層は標準化に吸収される側。残るのは検証者を自動化できない側の規律。この仮説は既存の「基盤層が標準化するほど責任オーバーレイの価値が上がる」という結論の実例であり、反証ではない。

---

## 1. 再導出層の定義変更

**現行**: 再導出層は、ROS2〜設備スタックを横断する責任境界として位置づけられている。形式化の対象として扱われている。

**変更後**: 再導出層を「まだ形式化できていない残り」ではなく「意図的に形式化しなかった領域」として再定義する。責任境界とは、どこまで形式化するかを決めた線そのものである、と定義し直す。

**根拠**: 書き下した基準は必ず最適化の的になる。エージェントループではこれが文字通り発現し、停止条件に向かって最適化した結果として仕様が壊れる。基準の一部を形式化しないでおくことは、ゴールを握らせきらないための設計上の選択であって、未完成ではない。

**影響**: 再導出層を扱う全記述。「いずれ形式化する」という含意を持つ表現があれば全て除去する。

---

## 2. 非決定の記録（新規・最重要）

**追加するもの**: 決定と同じ粒度で「決めないと決めたこと」を記録する枠。

**必須項目**:
- 形式化しない範囲の境界（どこからどこまでを空けたか）
- 空けると決めた主体
- 空けた理由
- 再評価の契機

**根拠**: 外形上、「意図的に空けた余白」と「基準を決めるのをサボった」は完全に同じ形をしている。監査でも区別できない。記録がなければ、意図的な余白は担当者の異動一回で属人性に変質する。

**独自性の所在**: 決定ではなく非決定を同粒度で記録する設計は、他のフレームワークが持たない非対称である。項目1と合わせて、ここがVCDesignの中核として残る部分になる。

**影響**: decisions.yaml に non-decision の記述枠を追加するか、別ファイルとして分離するかは実装判断に委ねる。ただし決定と同じ場所で読めることを条件とする。

---

## 3. 隣接境界観測ルール（新規）

**追加するもの**: 境界の一個外までを検証範囲に含めることを、選択ではなく制約として明記する。

**規則本体**:
- 境界の妥当性は境界の内側から観測できない。内側から見えるのは境界内の整合だけであり、境界の置き場所が誤っている場合の症状は必ず外側に出る
- したがって一個外の観測は作法ではなく、検証成立の必要条件である

**停止根拠（二個外を見ない理由）**:
- 一個外までは結果を誰の判断に帰属させるかを追えるが、二個外では因果が切れて帰属不能になる
- 加えて、各ノードが同じ規則で一個外を見ていれば、各境界は両側から観測されることになり、局所規則のまま連鎖全体が被覆される

**相互性条件（必須の対）**:
- 上記が成立するのは、隣接ノードも同じ規則を守っている場合に限る
- 連鎖内で一ノードでも規則を守らないと穴が空く。かつその穴は両隣から見えない。互いに相手が見ていると想定するため
- よって規則は「一個外を見る」単独では不完全であり、「隣も見ていることを確認する」とセットで記述しなければならない
- TPSで「後工程はお客様」と「不良を後工程に流さない」が必ず対で言われるのは同じ構造

**影響**: boundaries.yaml に隣接観測範囲と相互性確認の記述枠を追加。

---

## 4. 非連鎖結合の扱い（新規）

**追加するもの**: 距離規則が届かない結合クラスの列挙義務。

**内容**:
- 用力系（電源、エア、冷却水など）、同一ロット、同一作業者、共通ネットワーク・クロックといった共通原因は、二個先と直接つながる
- これらに対しては距離という概念が効かないため、項目3の規則では被覆できない
- 距離ではなく列挙によって押さえる

**影響**: 項目3と同じファイル内に、連鎖方向の規則とは別の条項として置く。項目3の下位項目にしないこと。

---

## 5. CCPの位置づけ変更

**現行**: CCPはVCDesignの派生プロジェクト。AI同士の接続時にIdentity / Boundary / Authorityを宣言するハンドシェイク仕様。

**変更後**: CCPを派生ではなく基底層として位置づけ直す。項目3の相互性条件の実装形であると明記する。

**根拠**: Identity / Boundary / Authority はいずれも接続の形式のみを宣言しており、接続される主体の性質を一切含まない。したがって層や規模が変わっても同一の形式が使える。AI間に適用できたのは擬人化が成立したからではなく、責任の受け渡しという操作の最小形を書いていたためである。AIを適用先としたのは、そこで最もむき出しになるからであって、本体ではない。

**影響**: VCDesignのレイヤー図におけるCCPの位置を下方に移動。「派生」「関連プロジェクト」といった表現を全て改める。

---

## 6. 導出順の反転

**変更内容**: CCPの説明順を「AI間接続の仕様 → 人の工程にも応用可能」から「人の工程間の接続 → AI間接続はその特殊例」に反転する。

**根拠**: 人の工程接続の方が先行事例として実在し、検証済みであるため。

**人の工程への適用にあたっての注意（記述に含めること）**:
- HELLO→OFFER→ACCEPT→REVERSE をそのまま持ち込まない
- 人の工程間では、引き継ぎ書やFMEAの形でハンドシェイクの一部が既に存在している
- 欠けているのはたいてい REVERSE、すなわち差し戻し条件の宣言である
- したがって最小適用は、既存の引き継ぎに「差し戻し条件が宣言されているか」だけを重ねる形になる。これを製造業現場への入口として明示する

---

## 7. 全体の位置づけ変更

**変更内容**: VCDesignを「設計記法」から「運用規律」に軸足を移す。

**根拠と判断材料**:
- 記法層（VC-AD / TDDD）はループ設計の共通言語として標準化され、吸収される見込みが高い
- 運用規律は責任の保持者に張り付くため吸収されにくい
- 製造業の調達フローに載るのは設計記法ではなく手順と監査である。適用先を製造業に限定した既存判断と整合する

**代償として記載すべきこと**:
- 記法は誰でも落として試せるが、規律は文脈を共有しないと伝わらない。配布可能性が下がる
- したがって記法層は廃止せず、規律への入口として残す

**影響**: サイト全体のトップ階層の説明文。VMSの位置づけは本変更の対象外（別途判断）。

---

## 8. 運用ポリシー集の構成規則（新規作成物）

項目3・4は、より大きなポリシー集の最初の2条にあたる。以降の条を追加する際の構成規則を先に固定する。

**各条に必須の項目**:
1. 規則本体
2. これを守らなかったときに何が起きたか（実例ベース）
3. なぜその範囲で止まるのか（適用範囲の上限とその根拠）

**立て方**: 原則からの演繹ではなく、破れ方から立てる。根拠のない心得の列になると、読み手が自分の状況に合わせて削れなくなる。

**素材**: 職務経歴の変化点ログ、および事故の初動に関する観察。

---

## 9. 未決事項（エージェントが決定しないこと）

以下は本仕様では確定していない。勝手に埋めず、未決として残すこと。

- 項目3の停止根拠を「帰属可能性」で確定してよいか。実務上の反例がないかの確認が必要
- 項目2の非決定記録を decisions.yaml 内に持つか別ファイルにするか
- 記法層（VC-AD / TDDD）をどこまで残すかの具体的な線引き
- VMSの位置づけ。本変更では扱わない
- ポリシー集の第3条以降

---

## 10. 適用範囲外

- idk-lamp.org は本変更の対象外
- ステークホルダー別の構造ビネット群は本変更の影響を受けない
- CCPの仕様本体（v2.1 Draft-Stable）のプロトコル定義は変更しない。変更するのは位置づけと導出順のみ
